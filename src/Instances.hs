module Instances where

import Data.EnumSet (EnumSet)
import Prettyprinter (Pretty, pretty)
import qualified Types.Type as T

-- instance Pretty (EnumSet T.Var) where
--   pretty set = pretty <$> toList set
